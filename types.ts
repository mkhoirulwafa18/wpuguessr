export interface Video {
    videoId: string,
    publishedAt: string,
    title: string,
    thumbnail: string
}

export interface LeaderBoardItem {
    name: string,
    socialMediaLink: string,
    labelSocialMedia: string,
    score: string
}